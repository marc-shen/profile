# Emacs package installations and native-compilation output are local caches.
emacs.d/elpa/
emacs.d/eln-cache/
emacs.d/var/
# Written by the Customize interface, so its contents describe one machine.
emacs.d/custom.el
