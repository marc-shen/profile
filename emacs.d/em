#!/bin/sh

if [ "$#" -eq 0 ]; then
    set -- "$HOME/"
fi

if emacsclient -n "$@" 2>/dev/null; then
    exit 0
else
    emacs "$@" >/dev/null 2>&1 </dev/null &
    command -v disown >/dev/null 2>&1 && disown
fi
